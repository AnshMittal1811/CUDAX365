
# PyTorch From Source Notes

- Clone: git clone --recursive https://github.com/pytorch/pytorch
- Install deps: pip install -r requirements.txt
- Build: python setup.py develop
- Optional: set USE_CUDA=1 for GPU builds.

This folder provides a quick sanity check script for local builds.
