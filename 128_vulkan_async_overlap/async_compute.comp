#version 450
layout(local_size_x = 16, local_size_y = 16) in;
layout(set = 0, binding = 0) buffer Buf { float data[]; } buf;

void main(){
    uint idx = gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * 256;
    buf.data[idx] = buf.data[idx] * 0.99;
}
