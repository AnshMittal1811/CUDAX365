#include <cstdio>

int main(){
    printf("Vulkan async compute + graphics stub.\n");
    printf("Compile SPIR-V with build_async.sh, then integrate into a Vulkan app with separate queues.\n");
    return 0;
}
